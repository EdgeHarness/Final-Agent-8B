<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
  <!-- PLACEHOLDER. Replace this file with the circled A lifted out of the real
       logo and the tab icon updates everywhere at once, since index.html and
       the header mark both point at this one file.
       A favicon has no page behind it to inherit a colour from, so unlike the
       inline header mark this one states its own, and carries a light variant
       for a light browser chrome. -->
  <style>
    .m { stroke: #ececec; }
    @media (prefers-color-scheme: light) { .m { stroke: #0d0d0d; } }
  </style>
  <g class="m" fill="none" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
    <circle cx="16" cy="16" r="14.6"/>
    <path d="M10.4 22 16 9.6 21.6 22M12.6 18.2h6.8"/>
  </g>
</svg>
